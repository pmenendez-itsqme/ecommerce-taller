/**
 * Marca de versión del código.
 *
 * ¿Para qué sirve? Para saber, MIRANDO LA PANTALLA, si el teléfono está
 * ejecutando el código que acabas de editar o una copia antigua en caché.
 *
 * Si cambias este valor, guardas, y la app NO muestra el valor nuevo,
 * entonces el problema no es tu código: Metro está sirviendo una versión
 * vieja, o estás editando una carpeta distinta a la que ejecuta Expo.
 */
export const BUILD_ID = 'etapa1-v3';
